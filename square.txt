		in
        pop rax
		in
        pop rbx
		in
        pop rcx

        push rax
        push 0
        je line

        call discr
        push rdx
        push 0
        jb two
        push rdx
        push 0
        je one
        jmp no
		ret

:discr  push rbx
        push rbx
		mul
        push rax
        push rcx
		mul
        push 4
		mul
		sub
        pop rdx
		ret

:one    push rbx
        push -1
		mul
        push 2
        push rax
		mul
		div
		out
		hlt

:two    push rdx
		sqrt
        pop rdx
        push rbx
        push -1
		mul
        push rdx
		add
        push 2
        push rax
		mul
		div
		out
        push rbx
        push -1
		mul
        push rdx
		sub
        push 2
        push rax
		mul
		div
		out
		hlt

:line   push rbx
        push 0
        je check
        push rcx
        push -1
		mul
        push rbx
		div
		out
		hlt

:check  push rcx
        push 0
        je inf
        jmp no

:no     push 78
		outc
        push 79
		outc
        push 95
		outc
        push 82
		outc
        push 10
		outc
		hlt

:inf    push 73
		outc
        push 78
		outc
        push 70
		outc
        push 95
		outc
        push 82
		outc
        push 10
		outc
		hlt