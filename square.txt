		in
        push rax
		in
        push rbx
		in
        push rcx
        pop rax
        push 0
        je 131
        call 43
        pop rdx
        push 0
        jb 83
        pop rdx
        push 0
        je 66
        jmp 162
		hlt
:43     pop rbx
        pop rbx
		mul
        pop rax
        pop rcx
		mul
        push 4
		mul
		sub
        push rdx
		ret
:66     pop rbx
        push -1
		mul
        push 2
        pop rax
		mul
		div
		out
		hlt
:83     pop rdx
		sqrt
        push rdx
        pop rbx
        push -1
		mul
        pop rdx
		add
        push 2
        pop rax
		mul
		div
		out
        pop rbx
        push -1
		mul
        pop rdx
		sub
        push 2
        pop rax
		mul
		div
		out
		hlt
:131    pop rbx
        push 0
        je 152
        pop rcx
        push -1
		mul
        pop rbx
		div
		out
		hlt
:152    pop rcx
        push 0
        je 183
        jmp 162
:162    push 78
		outc
        push 79
		outc
        push 95
		outc
        push 82
		outc
        push 10
		outc
		hlt
:183    push 73
		outc
        push 78
		outc
        push 70
		outc
        push 95
		outc
        push 82
		outc
        push 10
		outc
		hlt
